# Web框架
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.30.1
jinja2>=3.1.5,<3.2
starlette>=0.37.0,<1.0.0
python-multipart>=0.0.18

# 数据库
sqlalchemy>=2.0.30
# PostgreSQL 驱动（同步）
psycopg2-binary>=2.9.9
# PostgreSQL 驱动（异步，LangGraph checkpoint 使用）
asyncpg>=0.29.0

# LangChain 生态（text-splitters / huggingface 已拆分为独立包）
# 注意：langchain-deepseek 1.x 要求 langchain-core>=1.0.0，与 langchain 0.3.x 存在版本冲突，
# 但实际 API 兼容，pip 的依赖解析警告可忽略。Docker 构建时以 requirements-lock.txt 为准。
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0
langchain-community>=0.3.0,<0.4.0
langchain-text-splitters>=0.3.0
langchain-huggingface>=0.3.0
langchain-openai>=0.2.0
langchain-deepseek>=0.1.0
langgraph>=0.2.1,<1.1
langgraph-checkpoint-postgres>=2.0.0

# 向量 & Embedding
chromadb>=0.5.3
sentence-transformers>=3.0.1

# 文档处理
pypdf>=4.2.0
markdown>=3.6

# 工具
python-dotenv>=1.0.1
pydantic>=2.7.3
httpx>=0.27.0,<0.29.0

# Nacos 服务注册与发现
nacos-sdk-python>=2.0.0,<3.0.0

# Redis 限流与缓存
redis>=5.0.0

# 监控
prometheus-client>=0.20.0
